import { expect, fixture } from "@open-wc/testing";
import { DatePicker } from "../index";

describe("DatePicker", () => {
  describe("language", () => {
    it("should be equal language of browser when not assigned in constructor", async () => {
      // const container = new DatePicker({ value: "12/21/2021" });
      // const el = await fixture(container);
      // const langBrowser = document
      //   .getElementsByTagName("html")[0]
      //   .getAttribute("lang");
      // expect(labelTextEl.textContent).to.equal("");
    });
  });
});
